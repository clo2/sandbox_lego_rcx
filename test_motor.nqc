/*  出力 A と C のモータを前に数秒間だけ回転させるプログラム */

task main()
{
    OnFwd(OUT_A);     //  A 出力に接続されたモータを正回転させる
    OnFwd(OUT_C);     //  C 出力に接続されたモータを正回転させる
    Wait(1000);        //  プログラムの実行を特定秒待つ (単位は1/100秒)
                      //  この間 A と C のモータは回転したまま (現状維持)
    Off(OUT_A+OUT_C); //  A と C 出力のモータを止める
}
